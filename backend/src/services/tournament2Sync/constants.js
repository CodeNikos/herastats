/** Valor por defecto en local si no se define TOURNAMENT_2_SYNC_TARGET_TOURNAMENT_ID */
const DEFAULT_TARGET_TOURNAMENT_ID = 2;

const STEP_ORDER = ['teams', 'players', 'schedule', 'scores', 'events'];

module.exports = {
  DEFAULT_TARGET_TOURNAMENT_ID,
  STEP_ORDER
};
